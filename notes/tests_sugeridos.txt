__init__
    * data = np.array([])
    * data = np.array([1, 1, 1])
    * load_grid = (archivo inexistente)
    * load_grid = (sin data en diccionario), con data = np.array([])
    * load_grid = (sin data en diccionario), con data = np.array([1, 1, 1])
    * datos arbitrarios (control de asignación)

_build_grid
    * con datos arbitrarios, control de creación de la grid

_get_neighbor_cells
    * con datos random, control de tipo de salida
    * con datos random, control de formato de salida (elementos de la lista)
    * con datos arbitrarios, control lista de celdas
    * con datos arbitrarios, control lista de celdas (con periodicidad)

_mirror_universe
    * con datos random, control de tipo de terran_centres, terran_indices correctos    
    * con datos random, control de formato de terran_centres, terran_indices correctos
    * con datos arbitrarios, control de terran_centres, terran_indices correctos

_bubble
    * con datos random, control de tipo de neighbors_distances y neighbors_indices
    * con datos random, control de forma de neighbors_distances y neighbors_indices
    * con datos arbitrarios, control de neighbors_distances y neighbors_indices correctos

_shell
    IDEM ANTERIOR

_nearest
    IDEM ANTERIOR
